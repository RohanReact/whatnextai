# Optional frontend / Vite variables (copy to `.env` or `.env.local` in this folder).

# Used by vite.config.ts `define` when building — prefer keeping secrets only on the API server.
GEMINI_API_KEY=

# Base URL of the deployed app (optional; useful for links or integrations).
APP_URL=
